{{#if tips.length}}
	<ul class="tips-list">
		{{#each tip in enabledTips}}
			<li class="tip">
				{{#if tip.tooltip}}
					{{tip.tooltip}}
				{{else}}
					We will use {{tip.property}} for...
				{{/if}}
			</li>
		{{/each}}
	</ul>
	{{else}}
		No tips found
{{/if}}

{{yield}}
