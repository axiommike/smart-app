{{#if property}}
	{{#form-section title="Address and Location"}}
		{{#form-field title="Address"}}
			{{address-input address=property.address value=property.address.address placeholder="Property Address" required=true title="Please provide your property's address"}}
			{{#if property.address.address}}
				<dl class="address-info">
					{{#if property.address.unit}}
						<dt>Unit</dt>
						<dd>{{property.address.unit}}</dd>
					{{/if}}
					{{#if property.address.city}}
						<dt>City</dt>
						<dd>{{property.address.city}}</dd>
					{{/if}}
					{{#if property.address.province}}
						<dt>Province</dt>
						<dd>{{property.address.province}}</dd>
					{{/if}}
					{{#if property.address.region}}
						<dt>Region</dt>
						<dd>{{property.address.region}}</dd>
					{{/if}}
					{{#if property.address.country}}
						<dt>Country</dt>
						<dd>{{property.address.country}}</dd>
					{{/if}}
					{{#if property.address.postalCode}}
						<dt>Postal Code</dt>
						<dd>{{property.address.postalCode}}</dd>
					{{/if}}
				</dl>
			{{/if}}
		{{/form-field}}
		{{#form-field title="Do you have a rental agreement?"}}
			{{input type="checkbox" checked=property.isRental title="Please indicate whether this property was rented"}}
			{{#if property.isRental}}
				{{#form-field title="Rental Income"}}
					{{currency-input value=property.rentalIncome placeholder="Property Value" min="0" step="100"}}
				{{/form-field}}
			{{/if}}
		{{/form-field}}
		{{#form-field title="Estimated Property Value"}}
			{{currency-input value=property.value placeholder="Property Value" min="0" step="10000"}}
		{{/form-field}}
		{{#form-field title="Property Taxes"}}
			{{currency-input value=property.propertyTaxes placeholder="Property Taxes" min="0" step="any"}}
			{{#if property.propertyTaxes}}
				<span class="help">{{property.propertyTaxes}} * 12 = <strong>{{format-number property.propertyTaxesAnnual style="currency" currency="USD"}} per year</strong></span>
			{{/if}}
		{{/form-field}}
	{{/form-section}}
	{{#form-section title="Mortgage"}}
		{{liability-instance liability=property.mortgage typeEditingDisabled=true}}
	{{/form-section}}
{{else}}
	Hmmm... no property was found here.  Maybe try refreshing?
{{/if}}

{{yield}}
