{{wizard-step stepName="Liabilities" totalSteps=5 step=3}}

{{#validatable-form action="nextStep"}}
	<ol class="form-steps">
		<li class="form-step">
			{{#form-field title="Do you have any credit cards?"}}
				{{input type="checkbox" checked=hasCreditCard id="do-you-have-any-credit-cards?"}}
			{{/form-field}}
			{{#if hasCreditCard}}
				{{liability-table type="credit-card" liabilities=model.applicant.liabilities}}
			{{/if}}
		</li>
		<li class="form-step">
			{{#form-field title="Do you have a personal line of credit?"}}
				{{input type="checkbox" checked=hasLineOfCredit id="do-you-have-a-personal-line-of-credit?"}}
				{{#if hasLineOfCredit}}
					{{liability-table type="line-of-credit" liabilities=model.applicant.liabilities}}
				{{/if}}
			{{/form-field}}
		</li>
		<li class="form-step">
			{{#form-field title="Do you have any loans?"}}
				{{input type="checkbox" checked=hasLoans id="do-you-own-any-loans?"}}
				{{#if hasLoans}}
					{{liability-table type="loan" liabilities=model.applicant.liabilities}}
				{{/if}}
			{{/form-field}}
		</li>
		<li class="form-step">
			{{#form-field title="Do you have any other liabilities?"}}
				{{#if model.applicant.liabilities.length}}
					<span class="help">You have indicated that you have <strong>{{model.applicant.liabilities.length}}</strong> liabilities:</span>
					{{liability-table liabilities=model.applicant.liabilities}}
				{{/if}}
			{{/form-field}}
		</li>
	</ol>
	<footer class="form-actions">
		<button type="submit">Continue to Co-Applicants</button>
	</footer>
{{/validatable-form}}
{{outlet}}
